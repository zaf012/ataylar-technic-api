# Production Profile Configuration
# Application Configuration
server.port=${PORT:8080}
server.address=0.0.0.0
spring.application.name=ataylar-technic

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://213.159.6.92:3306/ataylar?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&failOverReadOnly=false&maxReconnects=10
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=adminzafer
spring.datasource.password=za190558

# Connection Pool Configuration
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:5}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:10000}
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.idle-timeout=${DB_IDLE_TIMEOUT:300000}
spring.datasource.hikari.max-lifetime=${DB_MAX_LIFETIME:600000}

# H2 Console (Disabled in production)
spring.h2.console.enabled=false

# Tomcat configs
server.tomcat.max-threads=50
server.tomcat.min-spare-threads=10

# Actuator (Limited endpoints for production)
management.endpoints.enabled-by-default=false
management.endpoint.health.enabled=true

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Logging Configuration (Production levels)
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.web=INFO
logging.level.com.ay_za.ataylar_technic=INFO
logging.level.org.springframework.security=WARN

# JPA Properties
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.id.new_generator_mappings=false

# Swagger/OpenAPI Configuration (Disabled in production)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=${API_DOCS_ENABLED:false}
springdoc.swagger-ui.try-it-out-enabled=false

# CORS Configuration for Cloud Run
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://yourdomain.com}
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Email Configuration
spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000

# Main Configs
spring.main.lazy-initialization=false

# Email Settings
app.email.from=${EMAIL_FROM:noreply@ataylar.com}
app.email.from-name=${EMAIL_FROM_NAME:ATAYLAR Technic}
app.email.enabled=${EMAIL_ENABLED:true}

# CORS
app.cors.allowed-origins=http://213.159.6.92,http://213.159.6.92:3000
app.security.ip-restriction.enabled=true

# PDF Storage Configuration (VPS server path for production)
pdf.storage.path=${PDF_STORAGE_PATH:/var/www/ataylar-technic/pdf-files}